# 安装依赖
```shell
pip install -r requirements.txt -i https://mirrors.cloud.tencent.com/pypi/simple
```

# 请求
```shell
curl "http://api.luoboedu.com/fs?action=calendar_update"
```

# 腾讯云 web ide 安装依赖
```shell
python310 -m pip install -r requirements.txt -t .
```
