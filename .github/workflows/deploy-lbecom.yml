name: lbecom MkDocs Deploy

on:
  push:
    branches: [ main, master ]
    paths:
      - 'lbecom/**'
      - '.github/workflows/deploy-lbecom.yml'

  pull_request:
    branches: [ main, master ]
    paths:
      - 'lbecom/**'

  # 手动触发：随时可以点击运行
  workflow_dispatch:
    inputs:
      clean_deploy:
        description: '是否清理COS目标目录'
        required: false
        default: false
        type: boolean
      deploy_message:
        description: '部署说明'
        required: false
        type: string

env:
  PYTHON_VERSION: '3.13'
  TZ: Asia/Shanghai # 设置当前环境时区
  COS_REGION: "ap-guangzhou"  # COS 区域
  COS_BUCKET: "lbecom-1251467080"  # 你的COS桶名称
  COS_PATH: "."  # COS上的目标路径


jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Sparse checkout lbecom directory
      uses: actions/checkout@v4
      with:
        fetch-depth: 1
        sparse-checkout: |
          lbecom/
          .github/workflows/
        sparse-checkout-cone-mode: false

    - name: Show directory structure
      run: |
        echo "📁 工作目录结构:"
        ls -la
        echo ""
        echo "📁 lbecom目录内容:"
        ls -la lbecom/ || echo "lbecom目录不存在"

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'
        cache-dependency-path: 'lbecom/requirements.txt'

    - name: Install dependencies
      run: |
        if [ ! -f "lbecom/requirements.txt" ]; then
          echo "❌ lbecom/requirements.txt 不存在"
          exit 1
        fi
        
        echo "📦 安装依赖..."
        pip install -r lbecom/requirements.txt
      working-directory: ./

    - name: Build MkDocs site
      run: |
        mkdocs build --site-dir public --verbose
      working-directory: ./lbecom

    - name: Configure AWS CLI for Tencent COS
      run: |
        pip install awscli
        mkdir -p ~/.aws
        echo "[default]
        region = ${{ env.COS_REGION }}
        output = json" > ~/.aws/config
        echo "[default]
        aws_access_key_id = ${{ secrets.COS_SECRET_ID }}
        aws_secret_access_key = ${{ secrets.COS_SECRET_KEY }}" > ~/.aws/credentials

    - name: Upload to Tencent COS
      run: |
        # 检查是否需要清理目标目录
        if ${{ inputs.clean_deploy == 'true' }}; then
          echo "🧹 清理COS目标目录: ${{ env.COS_BUCKET }}/${{ env.COS_PATH }}"
          aws --endpoint-url https://cos.${{ env.COS_REGION }}.myqcloud.com s3 rm s3://${{ env.COS_BUCKET }}/${{ env.COS_PATH }} --recursive
        fi
        
        echo "🚀 上传文件到COS..."
        aws --endpoint-url https://cos.${{ env.COS_REGION }}.myqcloud.com s3 sync ./lbecom/public/ s3://${{ env.COS_BUCKET }}/${{ env.COS_PATH }}/
        
        echo "✅ 部署完成！"
        echo "访问地址: https://${{ env.COS_BUCKET }}.cos-website.${{ env.COS_REGION }}.myqcloud.com/${{ env.COS_PATH }}"
      working-directory: ./

    - name: Deploy Notification
      if: always()
      run: |
        DEPLOY_STATUS=${{ job.status }}
        DEPLOY_MESSAGE=${{ inputs.deploy_message || '无特别说明' }}
        
        echo "📢 部署结果通知"
        echo "状态: $DEPLOY_STATUS"
        echo "说明: $DEPLOY_MESSAGE"
        echo "触发者: ${{ github.actor }}"
        echo "分支: ${{ github.ref }}"
        echo "提交: ${{ github.sha }}"